# 2026-06-14: Context Router v0.1

## Summary

Expanded the first common skill: `skills/context-router`.

## Added

- Study concept docs under `docs/concepts/`.
- Context routing request types.
- Required, optional, and missing context categories.
- Context expansion triggers.
- Source priority rules.
- Anti-patterns.

## Why

Context routing is the first foundation skill because every useful agent workflow depends on selecting the right files before acting.

## Next

The next common skill should likely be `generate-feature-context`, because it uses the routing result to build an implementation-ready context pack.
